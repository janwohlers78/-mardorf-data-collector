# Archive-v2 production proof

A full model validation on merge is required to prove the existing acquisition path against the newly promoted private `mardorf-weather-archive-v2` writer. The run must exercise provider acquisition, full native horizon sidecar, integrity audit, verified private transfer, private promotion and archive manifest creation.
