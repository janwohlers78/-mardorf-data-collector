Public acquisition stays provider-native and storage-agnostic. Verified model receipts carrying `full_horizon_archive` are promoted by the private repository, where `weather_archive.persist_archive()` writes `mardorf-weather-archive-v2`. Native horizon targets are defined in `src/full_horizon_contract.py`; analysis compatibility remains unchanged at <=120 h until a separate analysis-layer change.
