name: Validate secondary workflow definition

on:
  push:
    branches: [public-secondary-observations-20260920]

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo secondary-workflow-ok
